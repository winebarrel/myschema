init: |
  CREATE TABLE products (
      id INT NOT NULL,
      price INT NOT NULL,
      PRIMARY KEY (id),
      CONSTRAINT chk CHECK (price > 0)
  );

desired: |
  CREATE TABLE products (
      id INT NOT NULL,
      price INT NOT NULL,
      PRIMARY KEY (id)
  );

# allow_drop omitted → DROP CHECK suppressed.
plan: ""
disallowed_drops: |
  -- skipped: ALTER TABLE myschema_test.products DROP CHECK chk;
