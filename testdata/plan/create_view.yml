init: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      name VARCHAR(64),
      PRIMARY KEY (id)
  );

desired: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      name VARCHAR(64),
      PRIMARY KEY (id)
  );
  CREATE VIEW active_users AS SELECT id, name FROM users;

plan: |
  CREATE OR REPLACE VIEW myschema_test.active_users AS select `id`,`name` from `myschema_test`.`users`;
