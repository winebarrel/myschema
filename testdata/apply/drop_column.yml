init: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      legacy VARCHAR(64),
      PRIMARY KEY (id)
  );

desired: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      PRIMARY KEY (id)
  );

allow_drop:
  - column

applied: |
  ALTER TABLE myschema_test.users DROP COLUMN legacy;
