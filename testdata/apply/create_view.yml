init: ""

desired: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      name VARCHAR(64),
      PRIMARY KEY (id)
  );
  CREATE VIEW active_users AS SELECT id, name FROM users;

applied: |
  CREATE TABLE myschema_test.users (
      id bigint NOT NULL,
      name varchar(64),
      PRIMARY KEY (id)
  );
  CREATE OR REPLACE VIEW myschema_test.active_users AS select id, `name` from users;
