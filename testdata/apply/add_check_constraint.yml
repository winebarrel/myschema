init: |
  CREATE TABLE products (
      id INT NOT NULL,
      price INT NOT NULL,
      PRIMARY KEY (id)
  );

desired: |
  CREATE TABLE products (
      id INT NOT NULL,
      price INT NOT NULL,
      PRIMARY KEY (id),
      CONSTRAINT chk CHECK (price > 0)
  );

applied: |
  ALTER TABLE products ADD CONSTRAINT chk CHECK (price > 0);
