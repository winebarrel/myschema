# LIST end-to-end pin for the REORGANIZE value-change path.
init: |
  CREATE TABLE accts (
      id INT NOT NULL,
      region INT NOT NULL,
      PRIMARY KEY (id, region)
  ) PARTITION BY LIST (region) (
      PARTITION pAB VALUES IN (1, 2),
      PARTITION pCD VALUES IN (3, 4)
  );

desired: |
  CREATE TABLE accts (
      id INT NOT NULL,
      region INT NOT NULL,
      PRIMARY KEY (id, region)
  ) PARTITION BY LIST (region) (
      PARTITION pAB VALUES IN (1, 2, 7),
      PARTITION pCD VALUES IN (3, 4, 8)
  );

applied: |
  ALTER TABLE accts REORGANIZE PARTITION pAB, pCD INTO (
    partition pAB values in (1, 2, 7),
    partition pCD values in (3, 4, 8)
  );

verify_no_drift: true
