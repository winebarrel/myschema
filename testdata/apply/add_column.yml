init: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      PRIMARY KEY (id)
  );

desired: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      name VARCHAR(64) NOT NULL,
      PRIMARY KEY (id)
  );

applied: |
  ALTER TABLE users ADD COLUMN name varchar(64) NOT NULL AFTER id;
