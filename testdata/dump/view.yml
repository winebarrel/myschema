init: |
  CREATE TABLE users (
      id BIGINT NOT NULL,
      name VARCHAR(64),
      PRIMARY KEY (id)
  );
  CREATE VIEW active_users AS SELECT id, name FROM users;

dump: |
  -- users
  CREATE TABLE users (
      id bigint NOT NULL,
      name varchar(64),
      PRIMARY KEY (id)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

  -- active_users
  CREATE OR REPLACE VIEW active_users AS select `myschema_test`.`users`.`id` AS `id`,`myschema_test`.`users`.`name` AS `name` from `myschema_test`.`users`;
